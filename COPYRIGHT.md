# Copyright and licenses — secretstream55

This package is published under a dual license, Apache-2.0 or MIT, for
Lateos-authored files. The Apache-2.0 text is `LICENSE`. The MIT text is
`LICENSE-MIT` (Copyright (c) 2026 Lateos). The preamble of `LICENSE-MIT`
states that the MIT grant applies only to Lateos-authored files listed
below under « Fork-authored code ». Inherited wal-g material remains
under Apache-2.0 only, as already recorded in `NOTICE`.

## File classes

| File class | Origin | License |
|---|---|---|
| `*_gen.go` and files whose first line is `// Code generated by sgoiter` | Mechanical transpile from `monocypher.c` 4.0.2 (Loup Vaillant). Upstream licence copied from `/devhoros/c2simd/spec/c_sources/upstream/monocypher/4.0.2/monocypher.c` lines 1–52 into `LICENSE-MONOCYPHER`. | CC0 1.0 or BSD-2-Clause, recipient's choice |
| `internal/lsstream/` | Port of the lateos-ai fork of wal-g, Apache-2.0 (`NOTICE`) | Apache-2.0 only |
| `internal/monocypher/` | Monocypher 4.0.2 via ccgo (`a_linux_amd64.go` carries the ccgo stamp) | same upstream licence (`LICENSE-MONOCYPHER`) |
| Handwritten `hand_*.go`, `api.go`, `secretstream.go`, tests | This project | Apache-2.0 / MIT |
| `testdata/libsodium_interop/driver_secretstream.c` | This project. It links libsodium at compile time. | Apache-2.0 / MIT for the driver. libsodium is ISC. No libsodium source is copied into this tree. No ISC licence file is present in the captured sources; the ISC name is taken from the mission brief, not from a local copy of that licence. |
| `default.pgo` | Compilation profile, project data | project data (no third-party code) |

## Fork-authored code

`LICENSE-MIT` applies to the following Lateos-authored files, in
addition to `LICENSE`:

- `secretstream.go`
- top-level `*_test.go`
- `internal/engine/`
- `internal/monocypher/monocypher.go` and `internal/monocypher/monocypher_test.go` (handwritten wrapper and test around the ccgo emit)
- `internal/monocypher_sgoiter/shim.go`
- `testdata/libsodium_interop/driver_secretstream.c`
- `testdata/libsodium_interop/probe_state_layout.c` (when present)

`internal/lsstream/` is excluded from this list. It remains Apache-2.0
only, per `NOTICE` and the preamble of `LICENSE-MIT`.

## Files outside the classes above

- `README.md`, `CONTRIBUTORS.md`, `Makefile`, `.gitignore`
- `docs/WIRE_LIBSODIUM.md`
- `scripts/gen_goldens.sh`
- `testdata/libsodium_interop/README.md`
- `testdata/libsodium_interop/golden/` (interop vectors, project data, not ELF)
- `go.mod`, `go.sum`
- `NOTICE`, `LICENSE`, `LICENSE-MIT`, `LICENSE-MONOCYPHER`, `COPYRIGHT.md`
- `internal/engine/` (project handwritten code; Apache-2.0 / MIT; listed under Fork-authored)
- `internal/monocypher_sgoiter/shim.go` (same)
- `ROADMAP.md` when present (project prose)
