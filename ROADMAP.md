# Feuille de route secretstream55 / c2simd → amont golang/go#80881

État au 2026-08-25. Une case ne se coche que sur mesure (sortie de test, `cmp`,
`benchstat`), jamais sur rapport d'exécutant. Légende : `[✓]` mesuré vert ;
`[~]` entamé, preuve manquante ou test rouge ; `[ ]` non commencé ;
`[✗]` coché à tort, décoché sur oracle.

──────
### Phase 0 — Filet de Sécurité & Gardes Mécaniques

- [✓] G1. cmp universel de régénération : extension de `main.go` aux 12 artefacts `*_gen.go` avec réémission bit-exacte fail-closed.
  Coché 2026-08-26 (nuit) : `verify-all` rend 15/15 artefacts bit-exacts, `fyne55` compris (`gen_painter.go`, `c2_photometric_gen.go`, `gen_tt.go`).
  2026-08-25 (après-midi) : garde remanié — racine `c2simd` et binaire `sgoiter` paramétrés (`--c2simd`, `--sgoiter`, défauts dérivés de `--repo`), zéro chemin hôte dans `runVerifyArtifacts` ; test réécrit sur les 12 cibles nommées une par une, `sgoiter` construit dans le répertoire temporaire, deux négatifs (manuscrit sans en-tête ; artefact retouché après émission) et un fail-closed sans binaire. Vert `-race`.
  2026-08-26 : le garde rend 14/14 — les deux artefacts `fyne55` ont été régénérés dans leur arbre par un autre exécutant (non vérifié : lequel ; l'arbre `fyne55` n'a pas été touché par cette session).
  Passage sur les artefacts réels (`bin/sgoiter` reconstruit depuis la source courante) : 10/12 PASS. **Reste rouge** : `/devhoros/fyne55/pkg/c2painter/c2_photometric_gen.go` et `/devhoros/fyne55/pkg/tt55/gen_tt.go`, divergence = renommages `static` → non exporté (item 2.2) ; non régénérés parce que l'arbre `fyne55` porte un travail non commité d'origine inconnue. Commande prête : `bin/sgoiter -pkg <pkg> -in <src> -out <gen>` puis tests du paquet.
- [✓] G2. Module de contrôle négatif : `c2pkg/c2oracle`, assertion de rejet sur code/données mutés.
- [✓] G3. Module de détection d'aliasing : `c2pkg/c2alias`, prédicats `InexactOverlap` et `AnyOverlap`.
- [✓] G4. Portabilité, zéro chemin absolu : porte `verify-portability` (`portability_test.go`).
  Incident 2026-08-26 (soir), résolu : la garde `c2simd-fyne-guard verify-all` (motif `"/devhoros` — guillemet ouvrant + chemin, c'est-à-dire un littéral de chaîne commençant par un chemin hôte) a signalé à juste titre les deux `portability_test.go`, qui portaient ce littéral dans leur propre liste de motifs. La session Gemini (antigravity-cli, chantier fyne55/CI, 19 h 29) a réencodé les motifs en octets épelés mais a laissé tomber le guillemet ouvrant : le test interdisait alors la sous-chaîne `/devhoros` partout, donc le chemin de module `code.hazyhaar.fr/devhoros/…` de chaque import (17 violations dans `secretstream55`, 16 dans `monocypher55`, suites rouges). Correction de la session : guillemet rétabli dans les octets épelés, commentaire expliquant le motif ; les deux tests verts, `verify-all` 21/21 PASS (dont portabilité et 15 artefacts intègres). Leçon de harnais : une garde qui mord sur son propre test doit être contournée en épelant le motif, jamais en élargissant le motif.
- [✓] G5. Purge de la duplication : suppression de `c2simd/pkg` au profit de `c2pkg`.
  Décoché puis refermé 2026-08-25 : `cmd/c2vte` repointé sur `github.com/hazyhaar/c2pkg/c2tui` (résolu par le `go.work` du pôle, aucun `require` ajouté — voir 3.5) ; `internal/vtparser` supprimé (`git rm`) : jumeau plus ancien de `c2pkg/c2vtparser`, sans importeur, sans `extensions.go` ni `wcwidth.go`. `grep 'c2simd/pkg/'` sur les `.go` : aucune occurrence. Note : `c2pkg/c2ssim/` est encore non suivi par git (`??`).
- [✓] G6. Pincement strict sgoiter : retrait de `strings.Contains` dans `emit.go` au profit de correspondances exactes tranche ↔ longueur.
- [✓] G7. Tests de contrat secretstream55 : rejet de la troncature silencieuse et verrou d'erreur collant (`contracts_guard_test.go`).
- [✓] G8. Découplage des oracles C externes : tests C rejouables hors poste via `MONOCYPHER_C_SRC` ou `testdata/` embarqué, `t.Skip` explicite.
  Inventaire 2026-08-25 : le mécanisme existe déjà dans `pkg/monocypher55/testutil_test.go:17` (variable `MONOCYPHER_C_SRC`) avec `t.Skip` explicite ligne 45. Chemins hôte littéraux restants dans des tests : `c2simd/sgoiter/emit/split_test.go:63` (`/devhoros/pkg/monocypher55/monocypher_aead_sgoiter.go`), `c2simd/sgoiter/emit/overrides_test.go:13`, `c2simd/sgoiter/tribench/tribench_test.go:55-56`, `c2simd/sgoiter/sgoiterbench/sgoiterbench_test.go:8` (tous `/devhoros/c2simd`). L'occurrence de `c2blueteam/evasion_vectors_test.go:385` est un vecteur de données, pas une dépendance.
  Après-midi : les quatre sites résolus depuis le répertoire du paquet (`os.Getwd` + `../..`), le repli hôte de `tribench` supprimé (échec explicite), `overrides_test` vérifie la racine avant usage. `grep '"/devhoros/'` sur les `*_test.go` : aucune occurrence hors vecteur. Paquets `emit`, `tribench`, `sgoiterbench` verts `-race`.
  Clos 2026-08-26 (nuit), chantier Grok F1 (c), validé au sol : `cmd/c2simd-fyne-guard` et `cmd/c2simd-bench-probe` n'ont plus aucun littéral `"/devhoros` — racine `c2simd` dérivée (remontée au `go.mod`, ou `--c2simd`), `repo` = `<racine>/../fyne55` si présent, `cue-dir`, `c2pkg-dir`, racines de portabilité/provenance et `historyPath` dérivés (`resolve.go`) ; `TestNoHostPathLiteralInMains` et `TestVerifyAllExplicitRootsFromTemp` verts ; `verify-all` 22 PASS avec et sans `--repo`.
  Constat hérité : `TestSplitRealMonocypherFile` (`emit/split_test.go:62`) vise `pkg/monocypher55/monocypher_aead_sgoiter.go`, **absent du poste aussi** — le test était déjà un Skip silencieux avec le chemin absolu. À trancher : le repointer sur un artefact émis existant, ou le supprimer. Reste pour clore G8 : le passage en conteneur vierge (porte).
- [✓] G9. Harnais amont x/crypto (ajout 2026-08-25) — **fait le jour même**, `c2simd/upstream/xcrypto/` (`harness.sh`, greffe `chacha20poly1305_archsimd.go`, `results/benchstat.md` versionné). Tests amont (`TestVectors` 102/102, `TestRandom`) verts dans les trois configurations, fichiers de test prouvés intacts par `cmp` ; garde `go list` : sous `archsimd`, la greffe est compilée et l'assembleur ne l'est pas.
  **Mesure (i9-14900K, go1.27.0, cœurs 0-15, n=10)** — archsimd contre assembleur, débit : 64 B → 18–26 % ; 1 350 B → 38–41 % ; 8 192 B → 50–52 %. Contre `purego` : 64 B → −55 à −64 % ; 1 350 B → +32 à +44 % ; 8 192 B → +96 à +110 %. **Allocations : 736 B/op et 3 allocs/op à toutes les tailles** (assembleur 0/0, purego 32 B/1). Les chiffres du ticket (59–71 % de l'assembleur, « 0 B/op 0 allocs/op ») ne se reproduisent pas sur l'interface `cipher.AEAD` : ils venaient d'un harnais maison sur un autre point d'entrée. Voir 6.2.
  Définition initiale : copie figée de `golang.org/x/crypto@v0.54.0/chacha20poly1305` dans un module de test ; un seul fichier `chacha20poly1305_archsimd.go` sous tag `goexperiment.simd && amd64 && !purego` déléguant au noyau `c2pkg` ; `TestVectors` (102 vecteurs) et `TestRandom` amont exécutés **sans aucune ligne modifiée dans les fichiers de test** ; `BenchmarkChacha20Poly1305 -count=10` (longueurs amont 64, 1350, 8192 ; Seal/Open × nonce 12/24) dans les trois configurations asm / `-tags purego` / archsimd ; fichiers `benchstat` versionnés.
- [✓] G10. Sélection SIMD non silencieuse (ajout 2026-08-25).
  Fait le jour même : `monocypher55.SIMDActive()` (`simd_status.go`) expose la sélection réelle (build `goexperiment.simd && amd64` ET AVX2 au runtime) ; `secretstream55/simd_selection_test.go` la journalise et échoue si `SECRETSTREAM_REQUIRE_SIMD=1` alors que le repli est actif ; `bench_gate_test.go` se déclare non applicable (Skip motivé) sur le repli au lieu d'échouer sur un plancher calibré SIMD ; `Makefile` : cible `test` = `GOEXPERIMENT=simd SECRETSTREAM_REQUIRE_SIMD=1`, cible `test-fallback` = repli explicite. Trois comportements prouvés : SIMD exigé → vert ; repli sans exigence → gate Skip, suite verte ; repli avec exigence → échec. Suite complète `-race` verte sous la recette nominale.

│ Porte Phase 0 : suite complète verte dans un conteneur vierge sans `/devhoros` (`docker run golang:1.27`), 0 échec dur, 0 chemin hôte résiduel. Deux passages — avec et sans `GOEXPERIMENT=simd` — pour les modules à repli scalaire (`secretstream55`, `monocypher55`, `aeadengine`) ; la racine `c2simd` échoue à dessein sans SIMD (`require_simd_test.go`, fail-loud du pôle) et ne se mesure qu'avec.
│ Mesure 2026-08-25 matin (`GOEXPERIMENT=simd go test -race ./...` dans `c2simd`) : 17 verts, 4 rouges (G1, G5 ×2, 2.2). **Porte non franchie.**
│ **Passage en conteneur vierge 2026-08-26 (soir)** — `scripts/container_gate.sh`, `golang:1.27`, `GOPROXY=off`, cache de modules en lecture seule, arbres copiés sans `.git`. Première passe : trois défauts du harnais (`-mod=mod` refusé en mode workspace → `c2simd`/`c2pkg`/`aeadengine` non testés ; `monocypher55/go.mod` sans `require golang.org/x/crypto` — le workspace du poste masquait le manque, corrigé par `go get v0.54.0` + `tidy`, item 3.5 ; verdict global non agrégé → `RC` ajouté). Seconde passe : **vert** — racine `c2simd`, `aeadengine`, `cmd/c2simd-fyne-guard`, `kat`, `internal/*`, `sgoiter` et ses 9 sous-paquets sauf un, `c2pkg` 21/21, `monocypher55` avec et sans SIMD, `secretstream55` sous recette et en repli. **Rouges (4), tous hors périmètre crypto, couplés au poste** : `cmd/c2vte` (tests qui exécutent `./bin/c2vte` préconstruit), `p2go` (`TestAlgorithmsVsPhpOracle` exige `php-cli` ; `TestSimdParity` cherche un binaire nommé `go1.27.0` dans `PATH`/`~/go/bin` alors que `go` est déjà 1.27.0), `sgoiter/probebench` (`TestHostInventory` attend les montages du poste). Résidus de chemins hôte en dur (valeurs par défaut de drapeaux dans `cmd/c2simd-fyne-guard/main.go` et `cmd/c2simd-bench-probe/main.go`, scripts `sgoiter/scripts/*.sh`, `probebench/hostenv.go`) : listés par la porte, pas des tests. Chantier Grok B7 dispatché sur ces quatre rouges (G8).
│ **Troisième passe 2026-08-26 (soir), après B7 — PORTE PHASE 0 FRANCHIE : `RC=0`, 52 paquets `ok`, 0 échec.** B7 (validé sur le poste `-race`, puis par cette passe) : `cmd/c2vte` construit son binaire dans `t.TempDir()` ; `TestSimdParity` utilise le `go` courant s'il est 1.27.x ; `TestAlgorithmsVsPhpOracle` → `t.Skip` motivé sans `php` sauf `C2SIMD_REQUIRE_ORACLES=1` (recette du poste, `t.Fatal` conservé) ; `TestHostInventory` n'exige plusieurs montages que si `/devhoros` existe. Deux défauts du harnais corrigés en route : copie appartenant à root (artefacts construits par le conteneur) purgée avec `sudo -n` en repli. Reste ouvert sur G8 : les valeurs par défaut de drapeaux à chemins hôte dans `cmd/c2simd-fyne-guard` et `cmd/c2simd-bench-probe` (pas des tests, non bloquants pour la porte).
│ **Cinquième passe 2026-08-26 (nuit), après D3 (régénération de `monocypher55`) — PORTE PHASE 0 TENUE : `RC=0`, 53 paquets `ok`, 0 échec, avec et sans SIMD.** Deux défauts de harnais corrigés en route : la garde d'effacements de `monocypher55` forçait `GOWORK=/devhoros/c2simd/go.work` (chemin hôte hors motif de la garde de portabilité, qui n'attrape que les littéraux commençant par le chemin) → `GOWORK` hérité ; l'agrégation du verdict de `container_gate.sh` se perdait dans les sous-shells (faux `RC=0` en quatrième passe) → statut par fichier.
│ Mesure 2026-08-25 après-midi, après correction de G1, G5 et du harnais 2.2 : avec SIMD `-race` → **20 verts, 0 rouge** (exit 0) ; sans SIMD → 19 verts, 1 rouge attendu (`TestRequireGOEXPERIMENTSimd`, fail-loud de la racine). Troisième passe après G8 et suppression du résidu `c2simd/pkg/c2grid` : **20 verts, 0 rouge, exit 0** (rejouée par la session, pas relayée). La porte reste ouverte sur G9, G10, les 2 artefacts `fyne55`, l'arbitrage `TestSplitRealMonocypherFile` et le passage en conteneur vierge.

──────
### Phase 1 — Sûreté & Contrats sans Rupture de Format

État mesuré dans l'arbre le 2026-08-25 (adversarial : les cases vides sous-déclaraient le travail posé) :

- [✓] 1.1 Gardes de recouvrement mémoire (L1) : `c2alias.InexactOverlap` dans `Open`, `Seal`, `LockSubkeyDst`, `UnlockSubkeyDst`.
  Clos 2026-08-26 (chantier Grok A3) : prédicats internes `anyOverlap`/`inexactOverlap` (`internal/engine/alias.go`, sémantique x/crypto, aucune dépendance de module) ; gardes sur les quatre méthodes de `sgoiterEngine` rendant une **erreur** (contrat du module : verrou collant, pas panique) ; en place exact autorisé et prouvé bit-exact ; `dst`/`ad` et `dst`/`mac` refusés ; zéro allocation ajoutée. Fait corrigeant la prémisse de l'item : les couches basses paniquaient déjà sur recouvrement inexact `dst`/`plain` — il n'y avait pas de faux clair silencieux, mais un effondrement ; le seul silence réel était `dst`/`mac`. `TestContract_SubkeyAliasingOverlap` mis en cohérence (erreur au lieu de panique).
  Mesure : aucun appel à `InexactOverlap`/`AnyOverlap` dans le code non-test de `monocypher55`, `secretstream55`, `c2simd` racine, `aeadengine`. Un test de contrat existe déjà : `contracts_guard_test.go:103 TestContract_SubkeyAliasingOverlap` (vert). Reste : l'injection des gardes elles-mêmes.
- [✓] 1.2 Bornes de longueur et dépassement (L1) — deux fils distincts, tous deux opposables au 2026-08-26.
  Fil maison `[✓]` (2026-08-26, chantier Grok A : `secretstream_seq_overflow_test.go`, test de contrat vert, erreur collante vérifiée) — antérieurement `[~]` : garde de séquence présente dans `secretstream.go:124-126` (`if e.seq == ^uint64(0)` → erreur collante « sequence number overflow ») ; aucun test ne l'exerce (motifs `MaxUint64`, `SeqExhausted`, `overflow` absents des `*_test.go`) — il manque le test qui atteint la borne.
  Fil libsodium `[✓]` (2026-08-26, chantier Grok A2) : le débordement du compteur 32 bits n'est pas une erreur mais un rekey, exactement comme libsodium 1.0.18 — prouvé par T06–T09 de `internal/lsstream/rekey_transitions_test.go` contre le pilote C à compteur forcé (`cmp` du fil identique). Aucun correctif : `pushTo`/`pullTo` rekeyaient déjà sur `wrapped || TAG_REKEY`.
  Correction 2026-08-25 : la première mesure (« motifs non trouvés ») confondait absence de résultat de grep et absence de garde.
- [✓] 1.3 Cœur de déchiffrement in-place (L2) : absorption du fragment courant. Cible (non mesurée) : +60 % en place, parité stricte hors place.
  Clos 2026-08-26 (chantier Grok A7, validé au sol par la session). `ReadWithAD` déchiffre directement dans `p[:cipherLen]` quand `len(p) >= cipherLen` (`plainBuf` et `outBuf` non touchés) ; chemin reliquat inchangé. Rouge-avant `TestReadInPlace_NoIntermediateCopy` (`plainBuf` porte le clair dès le premier fragment) → vert ; `TestReadInPlace_ParityWithShortP` vert d'emblée (p grand et p = 7 rendent le même clair) ; `TestReadInPlace_NoPlaintextLeakOnBadMAC` : sur MAC invalide, `p` ne contient que le motif initial ou des zéros (le moteur par défaut déchiffre spéculativement puis efface `dst` sur le chemin long ; `c2simd` n'écrit pas `dst` en échec), erreur collante. **La cible « +60 % » n'est pas observée** : banc lecture 1 Mio 2 124–2 204 → 2 040–2 180 Mo/s, dans le bruit — la copie évitée pèse moins que le bruit de mesure à cette taille ; 1 alloc/op résiduelle sur le chemin chaud (`chunkNonce12` s'échappe via l'interface moteur, préexistant, hors item). `TestCloseWipesOwnedBuffers_Decryptor` avait une précondition périmée (« plainBuf sale ») : le second fragment y est désormais lu avec un tampon court pour passer par le chemin reliquat ; le contrat (`Close` efface `plainBuf`) est inchangé. Suite `secretstream55` `-race` verte sous recette hors `TestNoHostAbsolutePaths` (voir G4).
- [✓] 1.4 Alignement du compteur (L2) : `ctx.Counter` aligné sur la sémantique de l'oracle C.
  Clos 2026-08-26 (chantier Grok A6, supervision tranchée par la session). Divergence mesurée : `hand_aead_fused.go` avançait `ctx.Counter` après chaque message (`+= 2`/`+= 1` en chemin court, `= currCtr` en chemin long, write et read) là où `crypto_aead_write`/`read` de monocypher.c 4.0.2 ratchètent la clé (`auth_key[32:64]`) et laissent le compteur inchangé — premier message identique, second divergent. Rouge-avant `TestAEADStream_VsMonocypherC` (`aead_stream_c_test.go`, pilote C `gcc -O2` sur un même contexte, 9 tailles × AD {0, 13} × `init_x`/`init_djb`/`init_ietf`, write + lecture croisée dans les deux sens) : 18 sous-tests rouges au message 1 ; vert après retrait des quatre écritures du compteur. `TestAEADShortSIMDvsScalarAllSizes` était rouge après alignement parce que sa référence scalaire `refFastPathWrite` figeait l'ancienne avancée : la référence est alignée sur le C (commentaire daté), le contrôle d'état clé + compteur reste. Suites `monocypher55` et `secretstream55` (recette) vertes `-race` ; banc 1 Mo 2 310 → 2 240 Mo/s (bruit), 0 allocation. `secretstream55` non exposé (un `init_ietf` + un `write` par appel).
- [✓] 1.5 Garde de débordement Poly1305 (L3a) : garde de saturation dans `Poly_blocks`.
  Clos 2026-08-26 (chantier Grok A) : `monocypher55/poly1305_saturation_test.go` — `TestPoly1305SaturationH2_VsXCrypto` atteint `h2 > 3` (précondition vérifiée, clé et message 0xff × 1 024) et compare à x/crypto ; `TestPoly1305ParityVsXCrypto_10k`.
  Mesure : la garde `if h2 > 3` est présente dans `monocypher55/hand_poly1305_simd.go:80`. Aucun test nommé saturation/overflow/carry trouvé dans `monocypher55/*_test.go` : il manque le test qui échouait avant la garde.
- [✓] 1.6 Verrou d'erreur bidirectionnel (L4) : verrou collant lecteur et écrivain dès le premier échec.
  Clos 2026-08-26 (chantier Grok A) : `lsstream/writer.go` reçoit `stickyErr` (rouge-avant `TestWriterStickyErrAfterIOFailure` relevé dans le flux, vert après) ; `secretstream.go` : erreurs collantes sur tous les chemins d'échec de `WriteWithAD`/`ReadWithAD`.
  Mesure : `stickyErr` présent dans `secretstream55/secretstream.go` (Encryptor et Decryptor maison) et `internal/lsstream/reader.go` ; tests verts `TestContract_StickyErrorLibsodium` (:67) et `TestContract_StickyErrorMaison` (:177). Non trouvé dans `internal/lsstream/writer.go` : l'écrivain libsodium reste sans verrou.
- [✓] 1.7 Effacement de clé et `Close()` (L4).
  Clos 2026-08-26 (chantier Grok A) : `Close()` des deux côtés efface `subkey` et tous les tampons possédés (rouge-avant `TestCloseWipesOwnedBuffers_*`, vert après) ; le champ `key [32]byte` retenu par les structures est supprimé ; la clé d'appelant n'est pas effacée (décision de périmètre : mémoire de l'appelant). Attestation au désassemblage : 2.4.
  Mesure : `Encryptor.Close()` existe (`secretstream.go:89-96`) et efface la sous-clé (`clear(e.subkey[:])`). La `key` fournie par l'appelant n'est pas effacée après `HChaCha20` (:84), et aucun test ne vérifie l'effacement.
- [✓] 1.8 Rekey disjonctif libsodium (L5) : 11/11 transitions contre le C.
  Clos 2026-08-26 (chantier Grok A2) : pilote C étendu (`testdata/libsodium_interop/driver_secretstream.c`, commandes `push-script`/`pull-script`, `force-counter`, `rekey` hors bande ; disposition de l'état vérifiée par `probe_state_layout.c`), `TestInterop_RekeyTransitions/T01…T11` (`internal/lsstream/rekey_transitions_test.go`) : fil Go = fil C octet pour octet et lecture croisée dans les deux sens pour MESSAGE, PUSH, REKEY, FINAL, rekey explicite, compteur forcé à 0xFFFFFFFF sous chaque tag, message vide, FINAL plein/vide. Aucun correctif de production.
  Mesure : un seul test de transition, `internal/lsstream/roundtrip_test.go:34 TestAdvanceWrapDoesNotRekey` ; les 10 autres cas ne sont pas dans l'arbre.

│ Porte Phase 1 : 102/102 vecteurs x/crypto verts **via G9, sans modification des tests amont** ; 24/24 confrontations libsodium ; 0 allocation sur les bancs de flux.

──────
### Phase 2 — Transpileur sgoiter & Préservation d'Hygiène Mémoire

- [✓] 2.1 Préservation des `WIPE_CTX` / `WIPE_BUFFER` : 60 sites de remise à zéro de `monocypher.c` émis en écritures effectives non élaguées.
  Phase A close le 2026-08-26 (chantier Grok C1, rejoué au sol) : le compte exact est **65 lignes `WIPE_*` (75 objets)** — macro 2, émis 34, manuscrit 20, perdu/mixte 9. Aucun `clear` émis n'est élagué par le compilateur : garde `monocypher55/wipe_asm_test.go` (20 fonctions émises attestées au désassemblage + témoin négatif sans `KeepAlive` élagué, `scripts/wipe_probe.sh`). Les neuf sites manquants le sont **au source Go**, pas au code machine : `crypto_poly1305_final` (contexte 80 o), `crypto_blake2b_final` (224 o), `ge_scalarmult_base` ×3 (manuscrit), `crypto_eddsa_scalarbase` (Ge 160 o), `crypto_x25519_dirty_fast` (`pk`, `low_order_point`), `crypto_x25519_inverse` (`m_inv`) ; la voie SIMD manuscrite de HChaCha laisse l'état en registres. Les émis de `monocypher55` datent d'un émetteur antérieur (l'artefact récent `c2poly1305` porte bien `*ctx = T{}` + `KeepAlive`) : phase B = 2.5, régénération avec l'émetteur courant (brief `brief_mc55_regen_20260826.md`).
- [✓] 2.2 Traduction de la visibilité `static` : symboles Go non exportés.
  Clos 2026-08-26 (D2/D3) : les dix émis de `monocypher55` régénérés avec l'émetteur courant (aides `static` non exportées), manuscrits et tests alignés par 44 renommages mécaniques `gofmt -r`, globales sur le nom émis, règle `ExcludedSymbolVerbatimName` pour les symboles fournis en manuscrit ; seuls `Crypto_*` et `SIMDActive` sont exportés vers l'extérieur (4 noms mesurés).
  Appliqué dans `emit.go` (`uniqueGoName(…, static=true)` → `unexportName`) le 2026-08-25 à 13 h 03, hors phase. Harnais `tweetnacl_helpers_test.go` aligné (`ld32`), vert. Artefacts régénérés avec oracle : `c2pkg/c2ssim` (cmp identique, tests du paquet verts `-race`). Restent à régénérer : les deux artefacts `fyne55` (voir G1). L'item se cochera quand les 12 artefacts passent le garde et que 2.5 est joué.
- [✓] 2.3 Typage strict des tableaux et entiers : `[8]uint32` pour `L`, `[32]byte` pour `Lm2`, `[128]byte` pour `zero` (déjà acquis).
  Clos 2026-08-26 : règles CUE `FixedArrayLocalsAndFields` + `FixedArrayGlobals` ; tests `fixed_array_locals_test.go` / `fixed_array_globals_test.go` rouge→vert ; réémission bit-contrôlée de `monocypher55` (hunks : typage `L`/`Lm2` + tampon `sgoiter-stamp`) ; Gate 1b PASS (`verify-all` 23 PASS).
- [✓] 2.4 Garde de désassemblage wipe : test `go tool objdump` certifiant la présence physique des mises à zéro.
  Clos 2026-08-26 (chantier Grok B6, rejoué `-race` par la session). `c2simd/scripts/wipe_probe.sh` (fail-closed : binaire ou symbole absent → code 2/3) ; `c2pkg/c2poly1305/wipe_asm_test.go` : `TestWipeSurvivesInAsm_Poly1305Final` — après le dernier stockage vers `mac` (BX), les stockages de zéro (`X15`/immédiat 0) vers `ctx` (AX) doivent couvrir `[0, unsafe.Sizeof(ctx))` = 80 octets ; constaté : cinq `MOVUPS X15` à `0…0x40(AX)`. Contrôle négatif prouvé deux fois : `Crypto_poly1305_update` (un seul `MOVQ $0` de 8 octets) refusé ; témoin `wipeWitnessNoKeepAlive` (même effacement sans `KeepAlive`) compilé en `RET` nu — l'élagage est réel et `KeepAlive` est ce qui retient l'écriture. `c2simd/wipe_asm_test.go` : `polyTagSmall` n'est pas inliné, appelle `Crypto_poly1305_final` avec le contexte de pile (`0x38(SP)`) ; `AEADSubkeyLockDst`/`UnlockDst` appellent `polyTagSmall`. Aucun élagage sur les sites exigés. Limite connue : le critère dépend de l'ABI registre amd64 (ctx en AX, mac en BX) et les 360 lignes d'analyse sont dupliquées entre les deux fichiers de test.
  Précision 2026-08-25 : `c2pkg/c2poly1305/poly1305_gen.go:229` émet l'écriture de zéros du contexte puis `runtime.KeepAlive(ctx)` ; c'est une écriture émise, pas encore une attestation — `KeepAlive` réduit la probabilité d'élimination de l'écriture sans la garantir. Cible du garde : `Crypto_wipe` dans `c2poly1305` et le contexte de `polyTagSmall` (`c2simd/aead_subkey.go`).
- [✓] 2.5 Régénération globale des noyaux : réémission mécanique complète, aucune retouche manuscrite (un artefact à la fois, `cmp` après chacun).
  Première passe 2026-08-26 (chantier Grok C5, arrêt conforme au brief, annulée par la supervision : dix émis restaurés à HEAD, suites revertes ; sortie conservée sous `c2simd/bin/scratch/mc55_regen_C5/`). La régénération de `monocypher55` avec l'émetteur courant **rétablit six des neuf effacements perdus** (`poly1305_final`, `blake2b_final`, `eddsa_scalarbase`, `dirty_fast` ×2 en `*ctx = T{}` + `KeepAlive`, `m_inv` en `clear`) — les trois restants sont dans le manuscrit exclu `ge_scalarmult_base.go` — mais révèle **trois régressions de l'émetteur**, absentes de l'émission HEAD : (1) le tableau local initialisé `u32 m_inv[8] = {…}` (`monocypher.c:2800`) est hissé en global `M_inv` muté en place — état partagé, résultat faux au second appel ; (2) `runtime.KeepAlive` émis sans `import "runtime"` dans les fichiers scindés (`-split-dir`) ; (3) les symboles fournis par des manuscrits exclus sont appelés sous leur nom non exporté (2.2) alors que les manuscrits les exportent ; plus un doublon `Crypto_argon2_no_extras` (script `KEEP`). Porte Phase 2 non tenue (diff au-delà des effacements et de la visibilité : typage `uint8`→`byte`, SSA, `gap`/`rotl32`). Chantier D1 clos (validé au sol) : quatre règles CUE (`LocalInitArrayStaysLocal`, `SplitFileImports`, `ExcludedSymbolVerbatimName`, `ExcludeDropsMatchingGlobal`), quatre tests d'émission rouge→vert (`sgoiter/emit/regen_mc55_test.go`), fiche `F-sgoiter-regen-monocypher.cue`, `verify-all` 21/21 avec les 15 artefacts bit-identiques, `sgoiter/emit`, `rules`, `provenance` verts `-race`. La régénération est alors correcte sur les quatre défauts mais reste rouge sur la seule visibilité 2.2 : les aides `static` émises non exportées (`fe_sq`, `load32_le_buf`, `ge_double`, …) sont appelées sous leur ancien nom exporté par les manuscrits et les tests (`Fe_sq` ×25, `Fe_mul` ×19…). Décision de supervision : 2.2 s'applique, renommage mécanique des références par `gofmt -r` (identifiants, aucune logique touchée), les manuscrits qui définissent un symbole `-exclude` gardent leur nom ; seuls `Crypto_*` et `SIMDActive` sont utilisés hors du paquet (mesuré : 4 noms). Chantier D2 (arrêt conforme) : régénération + 44 paires `X -> x` par `gofmt -r` sur 46 fichiers manuscrits/tests (+ 18 copies testdata), sept globales alignées sur le nom émis, deux fonctions de test déplacées en paquet interne (`helpers_unexported_test.go`, assertions inchangées), `go build`/`go vet` propres, amalgame testdata vert, `verify-all` 21/21 ; **mais la suite reste rouge sur blake2b** : l'émission courante de `Crypto_blake2b` est sémantiquement fausse (vecteur RFC 7693 vide, oracle C, `x/crypto`), et Argon2 comme la dérivation de clé EdDSA en héritent — régression d'émetteur distincte des quatre corrigées (suspect à mesurer : la passe `astSinkMutatedInitArrays` de D1, ou la compaction SSA). L'arbre `monocypher55` est laissé en état régénéré + renommé pour le diagnostic ; `secretstream55` en dépend et est rouge tant que blake2b n'est pas rétabli. Chantier D3 en cours (sonde d'émission isolée de `crypto_blake2b_*` avec oracle RFC/gcc, bisection des passes, règle CUE, correctif, régénération). Lacune consignée : les dix émis de `monocypher55` ne sont pas dans la table du garde G1.

│ Porte Phase 2 : `git diff` de régénération limité aux effacements et à la visibilité ; oracle C `gcc -O2` ASan/UBSan 100 % vert ; survie des zéros attestée au désassemblage.
│ **Mesure 2026-08-26 (soir), chantiers C5 → D1 → D2 → D3, validés au sol.** Régénération réussie : `monocypher55` vert `-race` (9,8 s), `secretstream55` vert sous recette et en repli, `sgoiter` vert, `verify-all` 21/21 avec artefacts bit-identiques, `cue vet` vert. Cinq régressions de l'émetteur trouvées et corrigées, chacune par règle CUE + test d'émission rouge→vert : `LocalInitArrayStaysLocal` (tableau local initialisé hissé en global muté), `SplitFileImports` (`runtime`/`math`/`sync/atomic`/`syscall`/`archsimd` en mode scindé), `ExcludedSymbolVerbatimName`, `ExcludeDropsMatchingGlobal`, et **`SignedIntNotSignExtends`** — la cause de blake2b faux : `~(is_last_block - 1)` sur un `int` C était coercé en `uint32` par `ir.OpNot` quand le domaine de la fonction n'est pas 64 bits, perdant l'extension de signe attendue par `u64` (bisection par sonde isolée : `astSinkMutatedInitArrays` et `-split-dir` innocentés). Effacements : six des neuf sites perdus rétablis (`poly1305_final`, `blake2b_final`, `eddsa_scalarbase`, `dirty_fast` ×2, `m_inv` local), attestés au désassemblage (`wipe_asm_test.go` étendu) ; trois restent perdus dans le manuscrit `ge_scalarmult_base.go` (`tmp_a`…`s_scalar`) — à poser à la main dans ce manuscrit (résidu 2.1). Le diff de régénération dépasse « effacements + visibilité » : typage `uint8`→`byte` (2.3, partiel), SSA, `gap`/`rotl32` — la porte est tenue sur le fond (parité et oracles), pas sur la forme stricte du diff. ASan/UBSan : joué sur les oracles C des noyaux `c2pkg` (chacha8, poly1305x2), pas sur l'amalgame monocypher (non mesuré).

──────
### Phase 3 — Découpage Stratifié & Assainissement de Publication

- [✓] 3.0 Dépendance `secretstream55 → c2simd` supprimée (2026-08-25) : moteur déplacé dans `c2simd/aeadengine` (parité octet à octet contre x/crypto sur 22 tailles, avec et sans SIMD, `-race`) ; `secretstream55` exporte `Engine`, `NewEncryptorWithEngine`, `NewDecryptorWithEngine` (`engine_injection_test.go`) ; `go.mod` sans `require` ni `replace` vers `c2simd`. Preuve « module consommateur vierge » non jouée (`GOWORK=off` refusé par la garde de session) — reportée sur la porte.
- [✓] 3.1 Élagage du jumeau mort : suppression de `internal/monocypher` (7 397 lignes) et transfert de ses 17 fichiers de tests vers le moteur actif. Effet collatéral attendu : disparition des avertissements `go vet` `unsafe.Pointer` de ce dossier.
  Requalifié et clos par décision de l'usager le 2026-08-26 : `internal/monocypher` n'est pas un jumeau mort mais un **oracle vivant** (17 fichiers de tests, suite `-race` verte — `ok …/secretstream55/internal/monocypher 1.010s` le 2026-08-26) ; il est conservé comme oracle Go du moteur actif, aux côtés des drivers `gcc -O2` des `*_c_test.go`. Aucune suppression. Les avertissements `go vet` `unsafe.Pointer` de ce dossier restent à traiter séparément s'ils gênent une publication (3.5).
- [ ] 3.2 Découpage modulaire en 5 paquets autonomes (primitives ; ChaCha20/Poly1305/BLAKE2b/Argon2 transpilés ; noyaux AVX2 sous `goexperiment.simd && amd64` ; moteur AEAD ; Curve25519/EdDSA/Elligator dans un dépôt de parité dédié).
- [✓] 3.3 Suppression de la dépendance `x/crypto/poly1305` : `lsstream` sur le noyau interne validé.
  Clos 2026-08-26 (chantier Grok A4) : `internal/lsstream/secretstream.go` appelle `monocypher55.Crypto_poly1305_init/update/final` (émis par sgoiter) aux deux sites ; parité 2 000 tirages contre x/crypto avant remplacement (`poly_parity_test.go`) ; oracles rejoués verts (4 `TestInterop_*`, T01–T11, roundtrip, verrou) ; banc 8 Ko avant/après dans le bruit (10,3 → 10,7 µs à ±5 %), allocations inchangées (2). `x/crypto` reste requis pour `chacha20` ; `stage_bench_test.go` garde `x/crypto/poly1305` comme référence de banc.
- [✓] 3.4 Conformité légale : `LICENSE-MONOCYPHER`, `COPYRIGHT.md`, purge des binaires ELF suivis, en-têtes standardisés.
  Clos pour `c2simd` et `c2pkg` le 2026-08-26 (chantier Grok A8, validé au sol). Session : `LICENSE`/`LICENSE-MIT` copiés du sas (`cmp`) dans `c2simd`, `c2pkg` et `monocypher55` (qui ne les avait pas, contrairement au récit d'A5), `c2pkg/LICENSE-MONOCYPHER`. Grok : inventaire des 21 paquets `c2pkg` — une seule origine tierce émise (Monocypher → `c2poly1305`), les copies C tierces sous `sources/` (cJSON, yyjson, utf8proc, stb) n'alimentent aucun paquet ; `COPYRIGHT.md` des deux modules ; SPDX `Apache-2.0 OR MIT` sur 298 manuscrits (émis exclus par la règle `Code generated`, `spec/dogfood/testdata`, `upstream/xcrypto`, dumps `spec/c_sources/opt|raw`) ; 0 ELF suivi ; `go build`/`go vet` sans contrainte de build déplacée ; `gofmt -l` : 29 écarts préexistants non touchés. Vérifié par la session après le run : `c2simd-fyne-guard verify-all` 21/21 PASS, seize paquets verts `-race` (racine, `aeadengine`, `c2poly1305`, `c2chacha1/8`, `c2archsimd`, `sgoiter/...`). Résidu à qualifier : `c2pkg/c2archsimd/c2archsimd_nosimd_gen.go` est un manuscrit portant un suffixe `_gen` (a reçu un SPDX ; nom trompeur), et le greffon `upstream/xcrypto/chacha20poly1305_archsimd.go` reste sans en-tête (arbre x/crypto BSD-3, à aligner sur l'en-tête amont lors de 6.1).
  Fait pour `pkg/secretstream55` et `pkg/monocypher55` le 2026-08-26 (chantier Grok A5) : `LICENSE` et `LICENSE-MIT` copiés octet pour octet depuis le sas de publication (`cmp`), `LICENSE-MONOCYPHER` = bandeau de licence des lignes 1–52 de `monocypher.c` 4.0.2 (2 400 octets, identique dans les deux paquets), `COPYRIGHT.md` par paquet (classes de fichiers, origine, licence : émis sgoiter/ccgo ← Monocypher CC0/BSD-2, `lsstream` ← wal-g Apache-2.0, manuscrits ← Apache-2.0 OR MIT, pilote C ↔ libsodium ISC sans copie), SPDX `Apache-2.0 OR MIT` en tête de 79 fichiers manuscrits (émis exclus par règle ; tables générées `b_comb_*`, `b_window_table.go`, `a_linux_amd64.go` sans SPDX, à qualifier), 0 ELF suivi par git. Reste : `c2simd` et `c2pkg` (arbre occupé par le cycle 8 blocs au moment du run).
- [~] 3.5 Résolution hors arbre (ajout 2026-08-25) :
  **Preuve sans push acquise le 2026-08-26 (session)** : `c2simd/scripts/proxy35/prove35.sh` + `mkproxy.go` (x/mod zip) construisent un proxy `GOPROXY=file://` à partir de copies versionnées v0.1.0 des quatre modules, `replace` supprimé, `require github.com/hazyhaar/c2pkg v0.1.0` ajouté au `go.mod` de `c2simd` dans la copie (défaut réel : ce `require` manque, seul le `go.work` résout), `go 1.27rc1` → `go 1.27` (corrigé au sol dans `monocypher55` et `secretstream55`) ; consommateur vierge `example.org/consumer` (importe `secretstream55` + `c2simd/aeadengine`) : `go mod tidy`, `go build`, `go test` verts sans réseau. Cross-compilation : windows/amd64 vert ; linux/arm64 et js/wasm rouges (`undefined: archsimd.Uint8x32` — émis SIMD sous `goexperiment.simd` sans `&& amd64`) → chantier C4 en cours. Hygiène : `c2simd/.gitignore` rendait non suivables les sources C maison de `c2chacha1`, `c2chacha8`, `c2poly1305x2` et `test_poly1305_oracle.c` (G1 non rejouable depuis un clone) — exceptions ajoutées ; `spec/c_sources/upstream/monocypher/4.0.2/monocypher.c` reste ignoré (copie amont, décision de vendoring à prendre). **Preuve finale 2026-08-26 (soir), après C4/D3 : consommateur vierge `go mod tidy`/`build`/`test` verts depuis le proxy local, cross-compilation linux/arm64, js/wasm, windows/amd64 verte** (`prove35_final3.log`) ; deux pièges de harnais consignés dans le script — une extraction antérieure de la même version dans le cache de modules masque le zip regénéré (purge ciblée des quatre modules), et le cache de build doit être isolé (`GOCACHE`). Reste : la publication réelle (sas, hors « sans push »). Mesuré le soir : un `require github.com/hazyhaar/c2pkg v0.0.0` dans le `go.mod` réel casse la résolution même sous le `go.work` (le graphe de modules exige le `go.mod` de cette version) — le `require` ne peut être ajouté qu'avec une version réellement publiée ; jusque-là `c2simd` → `c2pkg` reste résolu par le workspace seul, et le script de preuve injecte `v0.1.0` dans la copie.
  Définition initiale : `secretstream55/go.mod` porte encore `replace monocypher55 => ../monocypher55` ; publier `monocypher55` (et `c2simd`, `c2pkg`) sous des versions résolubles et supprimer tout `replace`. Le `go.work` racine `/devhoros` référence de plus un `./pdfast` absent, ce qui casse `go build` sous ce workspace.
  2026-08-26 (chantier proxy35) : harnais reconstruit et suivi ; `./pdfast` retiré du go.work racine ; preuve rejouée : `go mod tidy`/`build`/`test` verts, cross linux/arm64, js/wasm, windows/amd64 verts ; reste : publication et retrait du replace (décision).

│ Porte Phase 3 : `go mod tidy` et `go build` sans réseau interne dans un module consommateur externe vierge (hors workspace) ; cross-compilation arm64, wasm, windows verte ; couverture ≥ 90 %.
│ Mesure 2026-08-26 (soir) : module consommateur vierge vert (voir 3.5). Cross-compilation — chantier Grok C4, validé au sol : règle CUE `SimdBuildTagAmd64` + `emit.go`, test d'émission `TestSimdBuildTagAmd64`, fiche `F-sgoiter-simd-buildtag-amd64.cue` ; `chacha1_gen.go`, `chacha8_gen.go`, `c2archsimd_simd_gen.go` réémis (diff = la ligne `//go:build goexperiment.simd && amd64` et le tampon) ; consommateurs manuscrits de la racine et `aeadengine` passés à `goexperiment.simd && amd64` / repli `!goexperiment.simd || !amd64` ; `doc.go` portable ajouté à `c2chacha1` et `c2chacha8` (paquets vides hors amd64, non importés hors amd64). Après : `c2archsimd`, `c2poly1305`, `c2poly1305x2`, `c2simd/aeadengine`, racine `c2simd` et `secretstream55` compilent sur linux/arm64, js/wasm et windows/amd64 ; `verify-all` 21/21 ; suites amd64 `-race` vertes. Hors nature portable, inchangés : `c2pty` (wasm), `c2q55` (`syscall.Mmap`, wasm), `secretstream55/internal/monocypher` (ccgo, `linux && amd64`). Couverture ≥ 90 % : non mesurée. **Porte Phase 3 tenue sur ses deux premières clauses le 2026-08-26 (soir)** : module consommateur vierge hors workspace et sans réseau, cross-compilation arm64/wasm/windows verte (preuve finale 3.5) ; 3.2 (découpage en cinq paquets) reste ouvert et non requis par la porte.

──────
### Phase 4 — Optimisation Fine des Noyaux SIMD

Ordre imposé par la mesure : l'écart à l'assembleur est sur les grandes charges (59–71 % à 1 KB–1 MB), pas sur les petites (89–94 % à 32–64 B, harnais maison ; valeurs à re-mesurer via G9). Un noyau à la fois, `benchstat` avant/après, contrôle nul.

- [✓] 4.0 Zéro allocation du chemin sous-clé (ajout 2026-08-25, préalable à 6.2). **(a) et (c) faits le 2026-08-25 ; (b) = cycle 4.1.**
  (a) `c2pkg/c2poly1305` : Poly1305 mono-chaîne **émis par `sgoiter`** depuis `spec/c_sources/upstream/monocypher/4.0.2/monocypher.c` (clôture de racines `crypto_poly1305_*`, exclusions, `-strip-dead-globals` ; tampon `00ee8b61…`), treizième artefact du garde G1 avec ses arguments d'émission. Oracles : vecteur RFC 8439 ; parité `x/crypto/poly1305` 0–1 088 octets, une passe et fragmenté ; oracle `gcc -O2` (`sources/test_poly1305_oracle.c`, 24 tailles) ; zéro allocation. 64 B : 41 ns contre 218 ns (constructeur QuadChain + MAC).
  (c) `c2simd/aead_subkey.go` : interface `polyMAC` et `newPolyMAC` supprimées ; chemin court (< `polySmallThreshold` = 512, croisement mesuré ~500 B) sur contexte émis, chemin long sur `Poly1305V2` concret sur pile ; `AEADLock/UnlockSIMD256_FusedDst` réduits à HChaCha20 + chemin sous-clé (doublon supprimé, équivalence testée). Sonde codegen : 0 accès pile YMM, bornes 10 → 4.
  **Oracle G9 après (a)+(c)** : `harness.sh all` — tests amont verts (3 configurations, fichiers de test intacts), **0 B/op, 0 allocs/op sur les 12 sous-bancs** (avant : 736 B, 3 allocs). Seal-64 574 → 377 ns (×5,7 → ×3,7 asm), Open-64 486 → 312 ns ; 1 350 et 8 192 inchangés (le noyau ChaCha, 4.1). Résultats versionnés dans `c2simd/upstream/xcrypto/results/`.
  Définition initiale : Oracle : `harness.sh bench` doit rendre 0 B/op, 0 allocs/op sur les 12 sous-bancs, parité G9 conservée. Origine mesurée par `go build -gcflags=-m` sur `c2simd` : `newPolyMAC` rend une interface et son état part au tas (`simd_poly1305_v2.go:424,427`) ; `sizeBlock` part au tas parce que passé à cette interface (`aead_subkey.go:78` et `:120`) ; `computedMac` idem (`aead_subkey.go:125`). Trois allocations par appel : deux dans `c2simd` (constructeur MAC 704 B, `sizeBlock`), une dans la greffe (`mac [16]byte` qui s'échappe parce que passé par pointeur à l'interface `polyMAC`).
  **Banc stratifié + compteurs matériels 2026-08-25** (`c2simd/upstream/xcrypto/results/stratified.md`) : à 64 B, 84 % du temps est hors keystream — constructeur MAC 186 ns (38 %, 3 603 instr, 704 B), bloc 0 par le chemin 8 blocs 109 ns (22 %, 1 849 instr pour 64 octets), résidu allocation/GC/interface 48 ns ; coût fixe 502 ns contre 94 ns pour l'assembleur. À 8 Ko, 69 % du temps est le noyau ChaCha20 (6,76 instr/octet, 51 % backend-bound). VZEROUPPER : 2,85–6,4 assistances/appel, ≤ 4 % des cycles — secondaire. Ordre d'attaque qui en découle : (a) constructeur MAC sans allocation ni puissances r² r³ r⁴ sous 1 Ko ; (b) bloc 0 par un chemin 1 bloc ; (c) `mac` sur pile (interface → type concret ou fonction) ; puis 4.1 sur le noyau ChaCha20.
- [ ] 4.1 Noyau ChaCha20 : forme émise pour le compilateur Go 1.27 (reformulé 2026-08-25 sur mesure — la transpilation s'optimise dans `sgoiter` par thésaurisation, avant toute fusion).
  **ERRATUM 2026-08-26 (16 h) — le banc stratifié mesurait un noyau mort.** L'étage s3 de `BenchmarkStrat` appelait `EncryptChaCha20_SIMD256` (manuscrit, plus appelé depuis le branchement c2chacha8). Banc corrigé (`xorKeystream8`) et rejoué : keystream réel **0,296 ns/o = AEAD asm complet 0,298** ; `perf` topdown (`rapport_c2simd_perf_topdown_20260826.md`) : `c2chacha8` IPC 2,84, memory_bound 15 % (déversements sans effet, mesuré), ports chargés par les rotations émulées ; `c2chacha1` IPC 1,45, core_bound 53 % (borné par la latence). À 8 192 B l'écart à l'asm (1 187 ns) = le MAC non recouvert (1 185 ns) ; à 1 350 B = MAC 318 + coût fixe 163 contre 103. **Le noyau 8 blocs est au plafond par octet : 4.1 « forme émise » est clos par la mesure.** Leviers restants, par ordre : re-seuil chemin court/long (Porte 4), `c2chacha2` bloc 0 + bloc 1 pour ≤ 64 B (latence), constructeur MAC, puis **fusion ChaCha–Poly (4.x) — plafond −32 % à 8 Ko, le seul levier de débit restant**.
  Mesure sur `c2simd/simd_stream.go:EncryptChaCha20_SIMD256` (manuscrit, 923 instructions, 904 octets de locaux) par `scripts/codegen_probe.sh` et `-gcflags=-m=2` :
  (1) `ChaCha20DoubleBlockSIMD256` **non inliné** (coût 441 > budget 240), appelé 2× par itération dans `for i := 0; i < 10` : les 4 registres YMM d'état passent par la pile à chaque appel (`args=0x80`), 48 accès pile YMM statiques dans l'appelant — c'est le déversement suspecté, mesuré ;
  (2) rotations 16 et 8 émulées en 3 instructions (`VPSLLD`+`VPSRLD`+`VPOR`, 8+8+8 dans le double-tour, 0 `VPSHUFB`) là où l'assembleur les fait en 1 (`vpshufb` ; masques déjà présents dans `monocypher55/chacha20_simd_amd64.go:24-30`) ;
  (3) 90 sites `panicBounds` dans la fonction (indexation de tranches non élidée dans le chemin chaud) ;
  (4) 0 `VZEROUPPER` émis dans une fonction AVX2 de 923 instructions — origine matérielle des 2,85–6,4 `assists.sse_avx_mix`/appel (6.3, secondaire).
  Le paquet émis `c2pkg/c2archsimd` (fonctions ≤ 130 instructions, sans boucle de tours) montre déjà (3) et 4 inlinings refusés ; le noyau de rounds est le bon banc d'essai pour (1) et (2).
  Précédent dans le magasin : `sgoiter/spec/findings/F-sgoiter-simd-direct-projections.cue` (`lever: handwrite`, `status: landed`) documente exactement cette forme — projections `Uint32x8` sans `StoreArray`, rotations 16/8 en `vpshufb`, compteur en accumulateur vectoriel — mais sur un noyau **écrit à la main** (`monocypher55/chacha20_simd_amd64.go:110`). La doctrine impose l'inverse pour la suite : émettre cette forme, puis thésauriser `lever: emit` ou `ir_rule` ; jamais recycler un `handwrite` en « transpilé ».
  **Cycle retenu (adversarial 2026-08-25, protocole 6 étapes, noyau étroit d'abord — c'est le chantier (b) de 4.0) :**
  1. Ingestion : source C d'**un bloc ChaCha20 sur 128 bits** (pas 8 blocs, pas Poly), oracle `gcc -O2`. Critère nommé : dans la boucle des tours, **zéro `VMOVDQU` vers la pile** (`objdump` / `codegen_probe.sh`) — un compte, pas un débit.
  2. À blanc : `sgoiter -in -out` ; inventaire des `skipped`/stubs. Interdiction d'un `hand_*.go`.
  3. Triptyque : le manque n'est pas une grammaire C mais une règle d'émission (projections `Uint32x4` sans `StoreArray`, rotations 16/8 en permute). La déclarer en CUE (`rules.cue` ou fiche) **avant** de l'écrire dans `emit.go` ; `cue vet`. Si elle ne s'unifie pas, le fait est mal posé.
  4. Parité : `Test*VsCOracle` bit-exact, puis `objdump` de la boucle. Si le déversement reste, l'étape 3 n'est pas close : on n'enchaîne pas.
  5. Fiche `F-sgoiter-chacha1-nospill.cue` : `status: landed` seulement avec `evidence.kat: pass` ancré au test et au compte d'accès pile ; `cue vet ./findings`.
  6. Non-antagonisme : `TestEveryDogfoodSourceCompiles` + `GOEXPERIMENT=simd go test -race`, régénération des 12 artefacts par le garde G1.
  **Cycle joué le 2026-08-25 (soir) — fiche `F-sgoiter-chacha1-nospill.cue`, `status: landed`, `cue vet` vert.**
  Étape 1 : `sources/c2chacha1/c2chacha1_simd.c` (un bloc, `__m128i`, rotations 16/8 en `pshufb`, 12/7 en décalage-ou, diagonalisation `_mm_shuffle_epi32`), oracle `gcc -O2 -mssse3` sur RFC 8439 §2.4.2 et 12 tailles LCG (`test_c2chacha1_oracle.c`). Étape 2 : à blanc, trois manques — quatre intrinsèques hors table, type des locaux `__m128i` capitalisé, chargements typés `uint32` (réglé côté C, tableaux d'octets). Étape 3 : six règles déclarées dans `spec/rules.cue` avant le code (`VectorLocalTypeVerbatim`, `MM_AddEpi32`, `MM_SlliEpi32`, `MM_SrliEpi32`, `MM_ShuffleEpi32`, `VectorLoopUnroll`), puis `emit.go` (goType, table) et `emit/vector_unroll.go` (déroulage reconnu sur l'IR, corps replié puis répété) ; test `sgoiter/vector_loop_unroll_test.go` rouge avant, vert après. Étape 4 : `c2pkg/c2chacha1` vert sur quatre oracles ; sonde : 0 accès pile dans la boucle des tours, `CALLpkg` 0 ; `perf` : assistances SSE/AVX 2,06 → 0 par appel, frontend-bound 24 % → 0,4 %. **Mesure : 133,6 → 66,4 ns par bloc** (x/crypto scalaire Go 71,8 ; scalaire émis `monocypher55` 86,8 ; noyau 8 blocs pour un bloc ≈ 106). Trois hypothèses réfutées en route et gardées dans la fiche (forme tableau des chargements, ligne 3 en mots, XOR vectoriel du bloc plein : aucune n'agissait — la cause était la boucle). Étape 6 : garde G1 étendu à 14 artefacts (en-tête admis après `//go:build`), 12/14 PASS ; suite `sgoiter` en cours.
  Branchement (b) dans `c2simd/aead_subkey.go` : bloc 0 et keystream ≤ 64 octets par `c2chacha1`, parité intacte (racine, `aeadengine`, amont, `secretstream55` sous recette). **Mesures après (a)+(b)+(c)** — harnais amont : Seal-64 574 → 222 ns, Open-64 486 → 221 ns, 0 B/op 0 allocs/op sur les 12 sous-bancs, 1 350 et 8 192 inchangés ; banc stratifié 64 B : 485 → 183 ns (×1,83 asm, contre ×4,9 le matin), s1 106 → 67, s2 186 → 8, s3 107 → 67, résidu 48 → 0. Le reste de l'écart à 64 B (≈ 80 ns) est la somme de deux blocs séquentiels (bloc 0 puis keystream) que l'assembleur fusionne ; à 1 350 et 8 192, c'est le noyau 8 blocs (4.1, même cycle). Suite `sgoiter` complète `-race` : 10 verts, 0 rouge (étape 6).
  Référence des mutations, du critère de non-rupture (tuyau lisse) et des usages `c2archsimd`/ARCHTIME : `c2simd/upstream/xcrypto/results/mutations.md` (2026-08-26) — ne remplace aucun item, fixe leur recevabilité.
  **Ordre arrêté après adversarial (2026-08-26)** — scission est/doit : doit rester dynamique tout ce qui dépend de la clé, du nonce, du compteur, du message ou de l'histoire (le recalcul de r²…r⁴ par appel est un choix de sûreté, jamais un cache) ; est dynamique par habitude : bornes sur `[]byte`, test `n == 64`, padding, seuil 512, `cap`, copies à gabarit fixe. Séquence : (1) **4.1 d'abord** — source C 8 blocs, témoin `gcc -O2 -mavx2`, grilles ARCHTIME du vecteur de compteurs `[0…7]` et des masques d'entrelacement (`unpack`/`permute2x128`) pour sortir 8 blocs sans `StoreArray`, même cycle que `c2chacha1` ; (2) en parallèle, tests de contrat `cap` (tampon de longueur 0 mais capable → 0 alloc et aliasing ; capacité insuffisante → allocation, contenu juste) ; (3) `FixedArrayParam` (`T nom[N]` → `*[N]T`) **seulement après** une sonde sur `c2chacha1` seul : `bounds` avant/après par `codegen_probe.sh`, et si `s1` du banc stratifié ne bouge pas, pas de règle globale ; l'entrée `xor_block64` ne vaut que si (3) a payé ; (4) garde de dérive des seuils 512/4 096 sous `-tags gate`, instrument, pas optimisation. Exclus : générateur de masques pour le bloc 1 (la RFC et l'oracle les tiennent — ne générer que ce dont la spécification n'existe pas ailleurs) ; descripteur CUE du format maison réservé à la Phase 5, après un oracle sur 4.1.
  **Cycle 8 blocs joué par Grok le 2026-08-26 (runs B, B3, B4, B5)** : étape 1 — `sources/c2chacha8/c2chacha8_simd.c` (16 registres `__m256i`, un mot par registre, grille de compteurs `[0…7]`, transposition `unpack`/`permute2x128` sans tableau intermédiaire), oracle `gcc -O2 -mavx2` RFC 114 octets + 18 tailles LCG, ASan/UBSan muets (vérifié par la session) ; étape 2 — inventaire : 7 intrinsèques hors table, `load32_le` hors clôture, chargement `uint32` typé octets ; étape 3 — onze règles déclarées en CUE puis implémentées (`MM256_AddEpi32`, `Slli/Srli`, `Set1Epi32`, quatre `Unpack`, `Permute2x128` à immédiat décodé, `VectorConstLoadTyped`, `StaticHelperInIntrinsicArgs`), test d'émission rouge→vert, l'émis compile ; étape 4 — **bloc plein : 3,16 Go/s, 5,56 instr/octet, 0 alloc, assistances SSE/AVX ≈ 0 — déjà au-dessus du noyau 8 blocs manuscrit (2,53 Go/s, 6,76 instr/octet)** ; mais fin partielle (`n % 64 ≠ 0`) en panique : l'émetteur a dédupliqué l'affectation `ks + 32` à travers les branches d'un `switch` (une seule affectation dans `case 0`, réutilisée ailleurs) — défaut de passe textuelle, étape 3 rouverte (B5 : reproducteur minimal, règle `NoCrossArmAssignDedup`, correctif, réémission). Sonde : 193 accès pile dans les tours, **structurels** (16 mots d'état + masques > 16 YMM) — le critère « zéro accès pile » tient sur `c2chacha1` (4 registres) et ne peut tenir ici par construction ; la voie « deux passes de 4 blocs » est un chantier de source C ultérieur. Garde G1 : 15 artefacts.
  **Étapes 5 et 6 closes et branchement joué (2026-08-26, session)** : `chacha8_test.go` borné aux compteurs que x/crypto accepte, débordement par voie (`0xFFFFFFF8/FC/FF`, 512 octets) prouvé contre le C seul (3 lignes `wrap:` de l'oracle) ; fiche `F-sgoiter-chacha8-nospill.cue` `status: landed`, `kat: pass`, `cue vet` vert ; garde G1 15/15. `c2simd/aead_subkey.go` : bloc 0 et ≤ 64 octets par `c2chacha1`, 65–511 par `c2chacha8.C2chacha8_xor_blocks`, chemin long par tranches de 4 096 via `xorKeystream8` (sonde : 0 accès pile YMM, `CALLpkg` 11, bornes 4). **Harnais amont après branchement** (`results/benchstat.md`) : 0 B/op, 0 allocs/op sur les 12 sous-bancs ; contre l'assembleur, Seal-64 218,7 ns / 109,7 (+99 %), Seal-1350 1 173 / 536 (+119 %), Seal-8192 4 039 / 2 641 (+53 %) ; contre la version précédente de la greffe : 8 192 −61 %, 1 350 −37 %, 64 stable. Banc stratifié : 64 B 193 ns (×1,89 asm), 1 350 B 1 019 ns (×1,94), 8 192 B 4 002 ns (×1,55) ; par octet 0,458 ns contre 0,304 (asm), keystream 0,412 ns/o (2,43 Go/s), MAC 0,148 ns/o. Poste dominant restant : le noyau ChaCha (86 % à 8 192), puis 1 350 B où le MAC (33 %) et le ctor MAC (102 ns, seuil 512 franchi) pèsent.
  **Second cycle joué le 2026-08-26 (nuit), chantier Grok E2, validé au sol — mesuré, non retenu** : `c2pkg/c2chacha4` émis depuis `sources/c2chacha4/c2chacha4_simd.c` (4 blocs par passe, état en 8 YMM, rotations `vpshufb`, diagonalisation `vpshufd`), une seule règle nouvelle (`MM256_ShuffleEpi32` → `PermuteScalarsGrouped`), quatre oracles verts (RFC, x/crypto 0..512, C 22 lignes dont 3 débordements, 0 alloc), fiche `F-sgoiter-chacha4-nospill.cue` landed, 16ᵉ artefact du garde, portable (arm64/wasm : paquet vide + `doc.go`). Sonde : **0 accès pile et 0 `MOVUPS` dans les tours** — le critère de forme est atteint. Banc (médianes, Mo/s) : 256 B `c2chacha4` 1 527 / `c2chacha8` 1 700 / x/crypto 975 ; 512 B 1 568 / **3 353** / 970. Verdict : deux passes de 4 blocs sans déversement sont deux fois plus lentes que la passe unique de 8 blocs qui déverse — le parallélisme d'instructions de 8 blocs vaut plus que le coût des déversements ; `c2chacha8` reste le noyau branché. L'écart résiduel à l'assembleur (0,41 contre 0,30 ns/octet) est donc la fusion ChaCha–Poly, pas la pression de registres. Leçon de harnais : un exécutant opencode ne peut pas lire le toolchain Go ; l'API `archsimd` doit être inlinée dans le brief.
  **Sonde `FixedArrayParam` (ordre 3 de l'adversarial), chantier Grok F1 (a), 2026-08-26 nuit** : règle CUE `FixedArrayParam` sous drapeau opt-in `-fixed-array-params`, émission de `c2chacha1` sondée sur copie (`bin/scratch/c1fap`, parité RFC/x/crypto/C verte) : `bounds` 20 → 16, instructions 670 → 643, banc 68,36 → 68,38 ns par bloc (bruit). **Verdict : sans effet mesurable, règle non généralisée.** Régression collatérale trouvée par la supervision (suite racine `sgoiter` non jouée par F1) : sans drapeau, les paramètres typedef `fe` sortaient en `*[10]int32` avec remise à zéro en `nil` — corrigée par F1b (`useFixedArrayParam` sous drapeau seulement, remise à zéro d'un `*[N]T` en `clear(h[:])`, `TestFixedArrayParamTypedefFe`, `sgoiter/...` complet vert, `verify-all` 22 PASS ; vérifié au sol : `fe_0(h []int32)`, 0 `nil` dans l'amalgame émis) ; leçon : tout changement d'émetteur exige `go test ./sgoiter/...` complet, `TestEveryDogfoodSourceCompiles` compris, pas seulement `verify-all`.
  **Garde de dérive des seuils (ordre 4), F1 (b)** : `aead_thresholds_gate_test.go` sous `gate && goexperiment.simd && amd64`, marge 10 % ; mesuré : chemin court 218/243/272 ns contre chemin long 293/279/296 ns à 448/512/576 octets ; tranche 4 096 = 26 179 ns contre 27 457 (2 048) et 26 572 (8 192) sur 64 Kio — les seuils tiennent.
   Fusion ChaCha–Poly : même cycle, après 4–5 verts sur ChaCha seul ; le C fusionné est un témoin gcc (plafond codegen) et un second dogfood, pas un livrable amont.
   Run 1 u128 scalaire (2026-08-26) : accès pile poly_blocks 95 → 21, seal_blocks 2 512 → 662.
    Run 2 (2026-08-26) : fusionné 4 379 ns contre réel 3 653 à 8 192 ; verdict mesuré, non retenu.
    Run 3 (2026-08-26) : panicBounds 640 → 175 ; fusionné 3 524 contre réel 3 651 à 8 192 ; verdict mesuré, non retenu.
    Run 4 (2026-08-26) : chemin long branché sur c2fused ; Seal 1 350 B 1 054 → 948 ns ; harnais amont Seal 64 / 1 350 / 8 192 archsimd 238 / 948 / 3 992 ns (45 / 56 / 64 % du débit asm).
    Run 5 (2026-08-26) : seuil 704 → 128 sur croisement court/fusionné (64..1024, quatre séries hors bruit dès 128, court gagne à 64) ; saut résiduel 64→128 +33 % (taille ×2 et c2chacha1→c2chacha8), pas suivant +7,7 %.
    Run 6 (2026-08-26) : panicBounds 175 → 126 ; accès pile GPR 419 → 406 ; Seal 8 192 B 3 690 → 3 435 ns (ratio asm 1,44 → 1,34×, 74,7 % du débit asm).
  **Clos par la mesure 2026-08-26 (chantiers c2fused_witness, uint128, inline_expand).** Témoin C `sources/c2fused` (Poly scalaire entrelacé dans les tours de `c2chacha8`, grille ARCHTIME générée) : **−21,1 % sous gcc** à 8 Ko. Émission Go : arithmétique 128 bits (`Uint128Arith`) et expansion des aides (`AlwaysInlineExpand`) posées ; l'artefact `c2pkg/c2fused` est bit-exact, sans `CALL`, alternance `MUL`/`VPADDD` conservée (542 sites) — et **2,6× plus lent** que le chemin réel (9 929 contre 3 812 ns) : 2 511 accès pile GPR, cadre 8 Kio, 657 `panicBounds`. **Relecture au sol (session, 26 août soir)** : la cause n'est pas l'allocateur mais la forme d'émission de `Uint128Arith` — chaque `u128` est un `[2]uint64` reconstruit à chaque opération, non décomposable en SSA (Go ne décompose que les tableaux de longueur 1), donc en mémoire ; `C2fused_poly_blocks` **seul**, sans aucun YMM, fait déjà 95 accès pile et 17 `panicBounds` par bloc pour 8 `MULQ`. Suspect suivant, décidable : émettre `u128` en deux locaux scalaires, hints de bornes corrects (`_ = 0` avorté), chargements LE en `binary.LittleEndian.Uint64` à mesurer : rapport `taskbooks/audits/buffers/rapport_sgoiter_inline_expand_20260826.md`. Point de données pour golang/go#79413 (« à mesure que le compilateur évolue »). (a) et (c) de 4.0 restent hors `sgoiter` (structure d'appel, oracle G9 0 alloc) et peuvent précéder ce cycle. Sans l'étape 5 le dogfood n'est pas clos ; sans l'`objdump` de l'étape 4, une cause est nommée avant d'être mesurée.
  Volet `__uint128_t` (2026-08-26, chantier uint128) : règle CUE Uint128Arith, émission `bits.Mul64`/`Add64`, `c2pkg/c2fused` 18ᵉ artefact, parité verte ; objdump 32 CALL `poly_step` et 0 entrelacement MUL/VPADDD ; banc 8 192 fused +4,5 % plus lent — mesuré, non retenu. Rapport : taskbooks/audits/buffers/rapport_sgoiter_uint128_20260826.md.
  Volet `AlwaysInlineExpand` (2026-08-26) : 0 CALL `poly_step`, 286 MULQ + 256 ADCQ dans le corps, 542 MUL/ADC entre VPADDD ; fused 8 192 médiane 9 929 ns contre LockDst 3 812 — mesuré, non retenu (déversements GPR). Rapport : taskbooks/audits/buffers/rapport_sgoiter_inline_expand_20260826.md.
  Fusion logicielle par tranches (2026-08-26, chantier fusion_soft) : V0…V3 à 8 192 Seal 3693/3678/3655/6126 ns ; verdict mesuré, non retenu (V2 −1 %, V3 +66 %) ; implication pour 4.x : le grain 512 ne recouvre pas, la fusion devra être instruction par instruction dans un seul noyau émis.
  Volet petites tailles (2026-08-26, chantier c4route) : F0 retenu (mesuré, non retenu). Lock médianes 64/128/191 ns F0 193/305/381, F1 309/341/408, F2 336/391/470 (0 B/op 0 allocs/op) ; F1/F2 plus lents de 116 ns à 64 B. Rapport : taskbooks/audits/buffers/rapport_c2simd_c4route_20260826.md.
  Volet deux blocs YMM (2026-08-26, chantier c2chacha2) : `c2pkg/c2chacha2` émis, 0 SP-YMM dans les tours, parité RFC/x/crypto/C verte ; fused128 146 ns contre c2chacha1 ×2 132 ns — mesuré, non retenu. Rapport : taskbooks/audits/buffers/rapport_c2chacha2_20260826.md.
  Volet excédent hors tours (2026-08-26, chantier c8excess) : instr 2840→2298, bounds 73→20, CALL encoding/binary 30→0, SP-YMM 306→329 ; 512 B 152,8→147,8 ns/op ; s3 8192 B 0,431 ns/o (cible 0,36) — mesuré, non retenu.
  Volet entrelacement (2026-08-26, chantier c8interleave) : keystream s3 8192 B 3317→3285 ns (0,405→0,401 ns/o, +1 % dans le bruit) ; SP-YMM 329→369 ; mesuré, non retenu. Sonde C (réutilisation d'identifiants vs temporaires SSA) : 1740 instr / 114 SP-YMM des deux côtés — sans effet mesurable.
- [✓] 4.2 Fast-path petits messages (65–191 octets) via chemin vectoriel 1 bloc `Uint32x4`.
  Mesuré et clos 2026-08-26 : `taskbooks/audits/buffers/rapport_c2simd_bench42_20260826.md` ; n ∈ {65, 96, 128, 160, 191, 256, 511} ; médianes ns/op c2chacha8 / 1-bloc = 136,2/132,2 ; 146,0/139,8 ; 136,3/136,8 ; 144,7/206,8 ; 163,6/218,5 ; 138,4/267,3 ; 170,1/548,9. Verdict : 4.2 périmé — `c2chacha8` reste ≥ au chemin 1 bloc sur 65–191 (delta ≤ 5 % ou en faveur de c2chacha8).
- [✓] 4.3 Recherche Poly1305 double accumulateur (r, r²). Cible (non mesurée) : 3,4 Go/s.
  **Mesuré et réfuté le 2026-08-26 (chantier Grok C2, validé au sol)** : `c2pkg/c2poly1305x2` émis par `sgoiter` depuis `sources/c2poly1305x2/` (12 fonctions, tampon `af210557…`, aucune règle d'émetteur nouvelle), six oracles verts (RFC 8439, x/crypto 0..1 088 une passe et fragmenté, 4 000 tirages contre la mono-chaîne émise, `gcc -O2`, 0 alloc, ASan/UBSan muets), fiche `F-sgoiter-poly1305x2.cue` landed, `verify-all` 21/21. Banc (Mo/s, médianes de 3) : 64 B mono 1 353 / x2 938 / asm 2 941 ; 1 350 B 2 058 / 1 389 / 4 315 ; 8 192 B 1 972 / 1 303 / 4 844. En scalaire émis, la forme (r, r²) coûte deux produits de corps et une addition à cinq membres par paire de blocs et **perd 30–35 %** contre la mono-chaîne : l'assembleur gagne par les voies SIMD, pas par l'algèbre. Le paquet reste comme témoin mesuré, non branché. Leçon de source C : `message + (i << 5)` en argument d'appel émettait `unsafe.Slice` et perdait l'avance ; la forme `message += 32` s'émet proprement.

│ Porte Phase 4 : latence monotone sur 0..2048 octets ; parité bit-exacte contre l'oracle C sur chaque variante ; gain validé contre contrôle nul.
  Mesure 2026-08-26 (chantier portes_p4_p6) : parité et gain acquis ; non monotone aux points listés (bruit 0,1–18 % par blocs de 64 o, saut de +140 à +290 ns au seuil polySmallThreshold = 512 o) — rapport : taskbooks/audits/buffers/rapport_portes_p4_p6_20260826.md.
  Re-seuil 2026-08-26 (chantier reseuil) : seuil 512 → 704 mesuré sur 448..2048 Seal/Open AD 0 et 13 ; saut au seuil 443→593 ns (+34 %) → 743→782 ns (+5 %) ; 1 350 B 974 → 1079 ns (chemin long inchangé) ; rapport taskbooks/audits/buffers/rapport_c2simd_reseuil_20260826.md.

──────
### Phase 5 — Spécification & Implémentation du Format de Flux v2

Méthode retenue (2026-08-26, `c2simd/upstream/xcrypto/results/mutations.md` §4) : le format maison est le seul dont la spécification n'existe pas ailleurs ; il se pose en descripteur CUE unique dont un générateur tire constantes Go, document et vecteurs. Réservé après un oracle sur 4.1.

Le mode libsodium (`NewLibsodium*`) est hors périmètre : sa valeur est l'identité octet à octet avec libsodium 1.0.18, prouvée par les quatre tests d'interopérabilité. Seul le mode maison (`NewEncryptor*`) est concerné. Un changement de filaire ne commence qu'après la spécification et ses vecteurs de référence.

- [✓] 5.1 Contrat injectif `WriteWithAD` : préfixe de longueur d'AD fixé avant publication.
- [✓] 5.2 Format v2 durci : nombre magique et en-tête versionné ; compteur monotone sans masque XOR ; `TAG_FINAL` authentifié obligatoire ; `Close()` obligatoire avec bloc terminal.
- [✓] 5.3 Décodeur hybride v1/v2 : reconnaissance à la lecture, rejet strict de toute archive v1 tronquée.
  **Clos 2026-08-26 (chantier Grok C3 + décision de supervision C3b, validé au sol).** Format v2 arrêté et implémenté (`secretstream.go`, `format_v2.go`, README § « Format de flux v2 ») : en-tête 36 octets = magic `SS55-v2\0` ‖ version u16 = 2 ‖ flags u16 = 0 ‖ nonce 24 ; sous-clé HChaCha20(clé, nonce[0:16]) ; nonce de trame `nonce[16:20] ‖ seq_be64` (monotone, sans XOR) ; trame `BE4 ‖ tag ‖ chiffré ‖ MAC16`, tag ∈ {MESSAGE, FINAL} authentifié ; AD = `"SS55-v2\0" ‖ seq_be64 ‖ tag ‖ len(ad)_be32 ‖ ad` (injective, domaine séparé du v1) ; `Close()` écrit la trame FINAL vide et est idempotent ; `Read` ne rend `io.EOF` qu'après FINAL authentifié, toute fin prématurée = erreur collante « flux tronqué ». `HeaderSize` = 36, `HeaderSizeV1` = 24 ; `NewDecryptor` reconnaît le magic (collision avec un nonce v1 aléatoire : 2⁻⁶⁴, documentée). `NewEncryptor` n'écrit plus de v1. Preuves : `v1_golden_test.go` + `testdata/v1/` (trois archives figées AVANT le changement, lecture bit-exacte après) ; `v2_attack_test.go` : 13 attaques en échec dur (troncature après trame / mi-trame, suppression, permutation, rejeu, tag MESSAGE↔FINAL, injectivité `("ab","c")` vs `("a","bc")`, longueur, MAC, chiffré, données après FINAL / second FINAL, en-tête version/flags/magic) + Close idempotent + EOF stable + allocations du chemin chaud inchangées. Sept tests existants décrivaient l'écrivain v1 : mis à jour sur décision de supervision, intention conservée (`Close()` avant lecture avec assertion « sans Close → flux tronqué », reconstruction indépendante du fil au format v2 pour les parités octet, `splitMaisonFrames` hybride, `TestContract_MaisonInterChunkTruncation_Phase5Deferred` → `…Truncation` avec l'assertion inversée : le trou est fermé). Suites sous recette et en repli vertes `-race`. Résidu : allocations du chemin chaud préexistantes (`Write` 2, `Read` 1 — `chunkNonce12` s'échappe via l'interface moteur), hors Phase 5.
  **ARCHTIME posé le 2026-08-26 (nuit), chantier Grok E1, validé au sol** : le format v2 est gouverné par `spec/format_v2.cue` (+ `spec/schema.cue`, `cue vet` vert) ; générateur `internal/formatgen` + `internal/cmd/formatgen` (`cue export` → trois produits : `format_v2_gen.go` avec les 15 constantes et `magicV2`, `FORMAT_V2.md` spécification en français, `testdata/v2/vectors.json` 4 vecteurs calculés avec le moteur par défaut) branché par `//go:generate` ; `format_v2.go` réduit aux fonctions, `secretstream.go` consomme les constantes générées. Gardes : `TestFormatCUEDrift` (régénération dans `t.TempDir()` + `cmp`, rouge prouvé sur mutation), `TestV2Vectors` (rejeu `NewEncryptor`+nonce injecté+`Close` octet pour octet). `go generate` idempotent ; suites recette et repli vertes `-race` ; 13 attaques + golden v1 inchangés (17 tests verts) ; aucun chemin hôte. Aucune valeur du format n'a changé.

│ Porte Phase 5 : 13 tests d'attaque structurelle en assertion d'échec dur ; lecture bit-exacte des archives v1 existantes.

──────
### Phase 6 — Contribution Amont (golang/go#80881)

État du ticket au 2026-08-25 : ouvert, aucun label, silence de l'équipe depuis le 2026-08-15. Le commentaire du 2026-08-15 (89–94 % sur petits messages) est contredit par les mesures ultérieures ; il doit être corrigé par l'auteur avant qu'un mainteneur ne le relève.

- [ ] 6.0 Commentaire de rattachement et de correction (dépend de G9 et de la porte Phase 1 seulement) : corps du ticket réécrit en markdown propre ; liens explicites vers #79413 (chantier « assembleur → SIMD natif », candidat suivant) et #76294 (point d'atterrissage x/crypto vs stdlib) ; `benchstat` du harnais amont sur 64/1350/8192 en trois configurations ; formulation en compromis maintenabilité / performance, non en « remplacement plus rapide » ; lien vers un chemin où le noyau est lisible seul.
- [ ] 6.1 CL Gerrit **sur demande d'un mainteneur uniquement** : paquet sous `golang.org/x/crypto/chacha20poly1305` avec `_generic.go`, `_archsimd.go`, `_noasm.go`, garde FIPS.
- [✓] 6.2 Métriques réelles sur l'interface `cipher.AEAD` — **mesurées par G9 le 2026-08-25** : 18–26 % de l'assembleur à 64 B, 38–41 % à 1 350 B, 50–52 % à 8 192 B ; 3 allocs/op et 736 B/op par appel là où l'assembleur n'alloue pas. Ces chiffres sont ceux à publier, avec la correction explicite des affirmations du 14 et du 15 août (« 0 allocs », « 59–71 % »). Reste avant publication : identifier et supprimer les 3 allocations (elles sont dans le chemin `c2simd.AEADSubkey*`, pas dans la greffe — voir 4.0), puis rejouer `harness.sh bench`.
  **Rejoué le 2026-08-26 09:52 UTC après 4.0 (a)(b)(c), c2chacha8 et 2.3** (`harness.sh all`, x/crypto v0.54.0, go1.27.0, `taskset -c 0-15`, `-count=10`, tests amont verts dans les trois configurations sans modification) — chiffres à publier, `Seal` nonce 12 : 64 B asm 101,1 ns / purego 198,6 / greffe 205,8 (≈ 49 % du débit asm, = purego) ; 1 350 B 489,5 / 1 736 / 1 054 (≈ 46 % ; −39 % vs purego) ; 8 192 B 2 364 / 9 374 / 3 766 (≈ 63 % ; −60 % vs purego) ; **0 B/op, 0 allocs/op** pour asm et greffe à toutes tailles (purego 32 B, 1 alloc). Source : `c2simd/upstream/xcrypto/results/benchstat.md`. Texte 6.0 assemblé sur ces chiffres : `taskbooks/audits/buffers/issue80881_comment_final_20260826.md`.
- [~] 6.3 Constat compilateur VZEROUPPER (surcoût de transition AVX/SSE à la frontière d'une fonction Go pure) : **ticket séparé** sur `cmd/compile`, programme minimal reproductible et désassemblage joints ; jamais dans le ticket crypto.
  Reproducteur posé le 2026-08-26 (chantier Grok B8b + mesures en root par la session) : module isolé `/devhoros/taskbooks/audits/buffers/repro_vzeroupper/` (`repro.go`, `repro_mixed.go`, tests de parité verts), rapport `/devhoros/taskbooks/audits/buffers/rapport_repro_vzeroupper_20260826.md` avec le texte du ticket en anglais. **Cause précisée, prémisse corrigée** : une boucle portée à quatre `Uint32x4` ne produit aucun `MOVUPS` en go1.27.0 ; ce sont les copies et déversements 128 bits sous pression de registres (seize valeurs vivantes) qui sont encodés en SSE hérité (`MOVUPS`, 20 par tour de `step16`), sans `VZEROUPPER` ; inoffensifs seuls (`LoopCarried16` : 135 ns, 0 assistance), ils coûtent une assistance chacun dès qu'une opération 256 bits vit dans la même fonction (`Mixed16`, un seul `Uint32x8.Add` par itération : 7 554 ns, ≈ 198 `assists.sse_avx_mix` par appel, ×56). Les copies 256 bits du même schéma sont en `VMOVDQU` (VEX) et ne coûtent rien. Reste : dépôt du ticket sur golang/go (décision de l'usager), et relecture de la fiche `F-sgoiter-chacha1-nospill.cue` à la lumière de cette cause (le gain 133 → 66 ns du déroulage venait de la suppression de la pression de registres, pas de la boucle en soi).

│ Porte Phase 6 : suite de tests amont intégrale sans régression sous `purego`, 386, arm64, amd64.
  Mesure 2026-08-26 (chantier portes_p4_p6) : exécuté vert sous amd64 (asm, purego, archsimd) et 386 (défaut, purego) ; compilé seulement sous arm64 (QEMU absent sur l'hôte, exclusion mécanique de la greffe vérifiée) — rapport : taskbooks/audits/buffers/rapport_portes_p4_p6_20260826.md.
